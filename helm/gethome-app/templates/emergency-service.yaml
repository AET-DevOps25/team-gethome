{{- if .Values.services.emergency-service.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: emergency-service
  namespace: {{ .Values.global.namespace | default "devops25-k8s-gethome" }}
  labels:
    app: emergency-service
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: emergency-service
  template:
    metadata:
      labels:
        app: emergency-service
        chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    spec:
      {{- if .Values.serviceAccount.enabled }}
      serviceAccountName: {{ .Values.serviceAccount.name | default "gethome-service-account" }}
      {{- end }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: emergency-service
          image: {{ .Values.services.emergency-service.image }}
          imagePullPolicy: {{ .Values.services.emergency-service.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.emergency-service.port }}
              name: http
          {{- with .Values.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.services.emergency-service.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if .Values.services.emergency-service.env }}
          env:
            {{- range .Values.services.emergency-service.env }}
            - name: {{ .name }}
              value: {{ .value | quote }}
            {{- end }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.services.emergency-service.port }}
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.services.emergency-service.port }}
            initialDelaySeconds: 30
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: emergency-service
  namespace: {{ .Values.global.namespace | default "devops25-k8s-gethome" }}
  labels:
    app: emergency-service
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
spec:
  type: {{ .Values.services.emergency-service.service.type }}
  selector:
    app: emergency-service
  ports:
    - port: {{ .Values.services.emergency-service.service.port }}
      targetPort: {{ .Values.services.emergency-service.service.targetPort }}
      protocol: TCP
      name: http
{{- end }}
