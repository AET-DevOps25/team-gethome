# Server Configuration
server.port=8082

# Database Configuration
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017}/usermanagement-service
spring.data.mongodb.database=gethome

# Auth Service Configuration
auth.service.url=http://localhost:8082

# JWT Configuration
jwt.secret=${JWT_SECRET:your-secret-key-here-make-it-long-and-secure-for-production}
jwt.expiration=${JWT_EXPIRATION:86400000}

# External Services Configuration
service.auth.url=http://auth-service:8081
service.message.url=http://message-service:8083
service.routing.url=http://routing-service:8084
service.ai.url=http://ai-service:8085

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:parthkarkar70@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:sfjb iopa hkmk gpmw}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always

# Logging Configuration
logging.level.org.springframework.security=DEBUG
logging.level.com.usermanagement_service=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security.web.FilterChainProxy=DEBUG 